════════════════════════════════════════════════════════════════
        🚀 SMART MEME COIN ANALYZER 🚀
        Complete Step-by-Step Running Guide
════════════════════════════════════════════════════════════════

📋 TABLE OF CONTENTS:
  1. Prerequisites Check
  2. Installation Steps
  3. Running the Application
  4. Testing the Application
  5. Troubleshooting
  6. Project Features

════════════════════════════════════════════════════════════════
STEP 1: PREREQUISITES CHECK
════════════════════════════════════════════════════════════════

✅ Check if you have:
   □ Python 3.11 installed (check: python --version)
   □ Virtual environment created (.venv folder exists)
   □ All files present in folder

📁 Required Files Checklist:
   □ app.py                          (Flask server)
   □ scraper.py                      (Data scraping)
   □ smart_meme_coin_model.pkl       (AI model - 11 MB)
   □ templates/index.html            (Main page)
   □ templates/analyze.html          (Results page)
   □ start_server.bat                (Startup script)
   □ requirements.txt                (Dependencies)

════════════════════════════════════════════════════════════════
STEP 2: INSTALLATION (First Time Only)
════════════════════════════════════════════════════════════════

Option A: Automatic Installation (RECOMMENDED)
───────────────────────────────────────────────
1. Open PowerShell in project folder
2. Run installation script:
   > .\install.bat

Option B: Manual Installation
──────────────────────────────
1. Open PowerShell as Administrator
2. Navigate to project folder:
   > cd "d:\university\Devopment\New folder"

3. Activate virtual environment:
   > .venv\Scripts\activate

4. Install dependencies:
   > pip install -r requirements.txt

5. Verify installation:
   > pip list

   Should show:
   - Flask
   - flask-cors
   - pandas
   - numpy
   - scikit-learn
   - xgboost
   - requests
   - beautifulsoup4

════════════════════════════════════════════════════════════════
STEP 3: RUNNING THE APPLICATION
════════════════════════════════════════════════════════════════

🎯 METHOD 1: Using Batch File (EASIEST)
────────────────────────────────────────
1. Go to folder: d:\university\Devopment\New folder
2. Double-click: start_server.bat
3. Wait for message: "Running on http://localhost:5000"
4. Open browser: http://localhost:5000

🎯 METHOD 2: Using Command Line
────────────────────────────────
1. Open PowerShell
2. Navigate to folder:
   > cd "d:\university\Devopment\New folder"

3. Activate virtual environment:
   > .venv\Scripts\activate

4. Run server:
   > python app.py

5. Look for output:
   ════════════════════════════════════════════
   🚀 SMART MEME COIN ANALYZER - BACKEND SERVER
   ════════════════════════════════════════════
   ✓ Model Status: Loaded
   ✓ Server starting on http://localhost:5000
   ════════════════════════════════════════════

6. Open browser: http://localhost:5000

🎯 METHOD 3: Using Python Directly
───────────────────────────────────
> "D:/university/Devopment/New folder/.venv/Scripts/python.exe" app.py

════════════════════════════════════════════════════════════════
STEP 4: TESTING THE APPLICATION
════════════════════════════════════════════════════════════════

📝 Test Scenario 1: Basic Test
───────────────────────────────
1. Open http://localhost:5000
2. You should see: "🚀 Meme Coin Analyzer" page
3. Input field should be visible
4. "Analyze Coin" button should be present

📝 Test Scenario 2: Analyze a Coin
───────────────────────────────────
1. Enter contract address:
   So11111111111111111111111111111111111111112

2. Click "Analyze Coin"

3. Wait 5-10 seconds (scraping + AI prediction)

4. Results page should show:
   ✅ Coin name and symbol
   ✅ BUY/SELL/HOLD prediction
   ✅ Confidence percentage
   ✅ Live DexScreener chart
   ✅ Price information
   ✅ Market data
   ✅ Trading activity

📝 Test Scenario 3: Additional Coins
─────────────────────────────────────
Try these addresses:
• EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v (USDC)
• 25Ew9oMprcypdK2KbecFE6Btvy5HChaDYUmLdm7aLinK (Sample)
• Any Solana token contract address

════════════════════════════════════════════════════════════════
STEP 5: STOPPING THE APPLICATION
════════════════════════════════════════════════════════════════

Method 1: Press Ctrl+C in terminal
Method 2: Close the PowerShell window
Method 3: Close batch file window

════════════════════════════════════════════════════════════════
STEP 6: TROUBLESHOOTING
════════════════════════════════════════════════════════════════

❌ PROBLEM: Port 5000 already in use
─────────────────────────────────────
SOLUTION:
1. Kill existing Python processes:
   > taskkill /F /IM python.exe

2. Or change port in app.py (line 309):
   app.run(debug=True, host='0.0.0.0', port=8080)

3. Then access: http://localhost:8080


❌ PROBLEM: Model not loading
──────────────────────────────
SOLUTION:
1. Check if smart_meme_coin_model.pkl exists (11 MB)
   > ls smart_meme_coin_model.pkl

2. Verify file size:
   > (Get-Item smart_meme_coin_model.pkl).Length / 1MB
   Should be ~11 MB

3. Test loading:
   > python -c "import pickle; f=open('smart_meme_coin_model.pkl','rb'); pickle.load(f)"


❌ PROBLEM: Module not found errors
────────────────────────────────────
SOLUTION:
1. Activate virtual environment:
   > .venv\Scripts\activate

2. Reinstall requirements:
   > pip install -r requirements.txt

3. Verify installations:
   > pip list


❌ PROBLEM: Browser shows "Connection Refused"
───────────────────────────────────────────────
SOLUTION:
1. Check if server is running (look for "Running on..." message)
2. Verify correct URL: http://localhost:5000
3. Try http://127.0.0.1:5000
4. Check firewall settings


❌ PROBLEM: "No data found" error
──────────────────────────────────
SOLUTION:
1. Check internet connection
2. Verify contract address is valid Solana address
3. Some new tokens may not be on CoinGecko yet
4. Try a known token like Wrapped SOL


❌ PROBLEM: Prediction shows ERROR
───────────────────────────────────
SOLUTION:
1. Check if all 107 features are being collected
2. Verify model loaded successfully
3. Check terminal for detailed error messages

════════════════════════════════════════════════════════════════
STEP 7: PROJECT FEATURES
════════════════════════════════════════════════════════════════

✅ FRONTEND:
   • Beautiful gradient UI
   • Responsive design
   • Real-time loading animations
   • Error handling with user-friendly messages
   • DexScreener chart embedding
   • Clean data visualization

✅ BACKEND:
   • Flask REST API
   • Model loading and caching
   • Real-time data scraping
   • Feature engineering (107 features)
   • AI prediction with confidence scores
   • Multiple data source integration

✅ DATA SOURCES:
   • DexScreener API (live prices, volume)
   • CoinGecko API (market data, rankings)
   • Birdeye API (Solana-specific data)
   • Social media metrics

✅ AI MODEL:
   • XGBoost Classifier
   • 89.20% test accuracy
   • 6 output classes (BUY, SELL, HOLD)
   • Confidence scoring
   • Probability distribution

════════════════════════════════════════════════════════════════
STEP 8: API DOCUMENTATION
════════════════════════════════════════════════════════════════

🔌 Endpoint: POST /api/analyze
───────────────────────────────
Purpose: Analyze a cryptocurrency

Request:
{
  "contract_address": "So11111111111111111111111111111111111111112"
}

Response:
{
  "success": true,
  "coin_data": {...},
  "graph_data": {...},
  "prediction": {
    "signal": "BUY",
    "confidence": 85.5,
    "probabilities": {...},
    "recommendation": "Strong buy signal..."
  },
  "timestamp": "2026-01-01T16:00:00"
}

🔌 Endpoint: GET /api/health
────────────────────────────
Purpose: Check server status

Response:
{
  "status": "healthy",
  "model_loaded": true,
  "timestamp": "2026-01-01T16:00:00"
}

════════════════════════════════════════════════════════════════
STEP 9: DEVELOPMENT TIPS
════════════════════════════════════════════════════════════════

💡 Debugging Mode:
   app.py already has debug=True enabled
   Changes will auto-reload server

💡 View Logs:
   All logs appear in PowerShell terminal
   Check for errors and warnings

💡 Test API Directly:
   Use Postman or curl to test endpoints
   Example:
   > curl -X POST http://localhost:5000/api/analyze ^
     -H "Content-Type: application/json" ^
     -d "{\"contract_address\":\"So11...112\"}"

💡 Modify Port:
   Edit app.py line 309:
   app.run(debug=True, host='0.0.0.0', port=YOUR_PORT)

💡 Add More Features:
   • Edit scraper.py to add data sources
   • Modify prepare_features() in app.py
   • Update frontend in templates/

════════════════════════════════════════════════════════════════
STEP 10: DEPLOYMENT (Production)
════════════════════════════════════════════════════════════════

🌐 For Production Deployment:

1. Install Gunicorn:
   > pip install gunicorn

2. Run with Gunicorn:
   > gunicorn -w 4 -b 0.0.0.0:5000 app:app

3. Use Nginx as reverse proxy

4. Set debug=False in app.py

5. Add proper error logging

6. Use environment variables for config

════════════════════════════════════════════════════════════════
✅ QUICK START CHECKLIST
════════════════════════════════════════════════════════════════

□ Step 1: Check all files exist
□ Step 2: Open PowerShell in project folder
□ Step 3: Double-click start_server.bat
□ Step 4: Wait for "Running on http://localhost:5000"
□ Step 5: Open browser to http://localhost:5000
□ Step 6: Enter contract address
□ Step 7: Click "Analyze Coin"
□ Step 8: View results with live chart
□ Step 9: Test with multiple addresses
□ Step 10: Enjoy your AI-powered crypto analyzer!

════════════════════════════════════════════════════════════════
📧 NEED HELP?
════════════════════════════════════════════════════════════════

Common Issues:
• Check README.md for detailed documentation
• Verify all dependencies installed
• Ensure model file is 11 MB
• Check internet connection for scraping
• View terminal for error messages

════════════════════════════════════════════════════════════════
🎉 YOUR PROJECT IS READY!
════════════════════════════════════════════════════════════════

Just run: start_server.bat
Then open: http://localhost:5000

Happy Analyzing! 🚀💰

════════════════════════════════════════════════════════════════
